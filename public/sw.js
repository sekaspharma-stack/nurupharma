const CACHE_NAME = 'pharmasys-shell-v13';
const SHELL = ['./','./index.html','./favicon.svg','./manifest.webmanifest','./icon-192.png','./icon-512.png','./sw.js'];
const SUPABASE_CDN = 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2';
self.addEventListener('install', event => { event.waitUntil((async()=>{ const cache=await caches.open(CACHE_NAME); await cache.addAll(SHELL); try{const r=await fetch(SUPABASE_CDN,{mode:'cors',cache:'no-store'});if(r.ok)await cache.put(SUPABASE_CDN,r.clone());}catch(e){} await self.skipWaiting(); })()); });
self.addEventListener('activate', event => { event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE_NAME).map(k=>caches.delete(k)))).then(()=>self.clients.claim())); });
self.addEventListener('fetch', event => { const req=event.request; if(req.method!=='GET')return; if(req.mode==='navigate'){ event.respondWith((async()=>{try{const res=await fetch(req); const c=res.clone(); caches.open(CACHE_NAME).then(x=>x.put('./index.html',c)); return res;}catch(e){return (await caches.match(req))||(await caches.match('./index.html'));}})()); return;} const url=new URL(req.url); if(url.origin===location.origin){ event.respondWith(caches.match(req).then(cached=>cached||fetch(req).then(res=>{const c=res.clone();caches.open(CACHE_NAME).then(x=>x.put(req,c));return res;}))); } else if(url.href===SUPABASE_CDN||url.hostname.includes('jsdelivr.net')){event.respondWith(fetch(req).then(res=>{const c=res.clone();caches.open(CACHE_NAME).then(x=>x.put(req,c));return res;}).catch(()=>caches.match(req)));}});
